class ApiConfig {
  // Use local address for testing if necessary, but using the deployed one as requested.
  // For local Android emulator testing, use 'http://10.0.2.2:3000/api'
  // For local iOS simulator testing, use 'http://localhost:3000/api'
  // For local physical device, use 'http://<your-ip-address>:3000/api'
  static const String baseUrl = 'https://decoration-rent-omega.vercel.app/api';
}
